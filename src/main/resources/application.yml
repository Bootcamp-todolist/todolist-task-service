server:
  port: 9093

spring:
  flyway:
    enabled: true
    url: jdbc:mysql://localhost:3307/todolist-task?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_bin&useServerPrepStmts=false&rewriteBatchedStatements=true&createDatabaseIfNotExist=true
    user: root
    password: changeme
  datasource:
    url: jdbc:mysql://localhost:3307/todolist-task
    username: root
    password: changeme
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      ddl-auto: validate
